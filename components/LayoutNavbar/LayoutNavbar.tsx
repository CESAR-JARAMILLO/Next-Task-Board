import { Box } from "@mantine/core";
import styles from './LayoutNavbar.module.css';

interface LayoutNavbarProps {
  
}

const LayoutNavbar = ({}) => {
  
  return (
    <Box className={styles.layoutNav}>
      Navbar
    </Box>
  );
}

export default LayoutNavbar;
